<div class="discourse-bookmark-button-wrapper">
  {{!-- 使用内置的 DButton / d-button 风格按钮 --}}
  <DButton
    class={{this.buttonClass}}
    @icon={{this.icon}}
    @title={{this.tooltip}}
    @label={{this.label}}
    {{on "click" this.toggleBookmark}}
  />
</div>
